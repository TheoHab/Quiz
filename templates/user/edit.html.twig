{% extends 'base.html.twig' %}

{% block title %}Edit Users{% endblock %}

{% block body %}

    	<div class="card d-block m-auto bg-dark text-light" style="width: 25rem;">
		<h5 class="card-header">Modifiez votre profil</h5>
		<div class="card-body">
			<h5 class="card-title"></h5>
			<p class="card-text">
                {{ include('user/_formEdit.html.twig', {'button_label': 'Update'}) }}
			</p>
			<button type="button" class="btn btn-primary">
                <a style="color: white" href="{{ path('app_user_index') }}">back to list</a>
			</button>
            {{ include('user/_delete_form.html.twig') }}
		</div>
	</div>

{% endblock %}
